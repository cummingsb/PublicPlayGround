WriteThis
	This code will take user input and create a polyline featureclass call MyLines (in a virtual workspace).
	
	Tool will be under 'Bretts Controls'. The button will launch a window that allows the user to input text and create a line feature(s).

	Target: ArcMap 10.5.1
	Dependencies: Map must be in wgs 84.

	Development env: Visual Studio 2013
